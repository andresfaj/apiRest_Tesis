import { FilterpublicationsPipe } from './filterpublications.pipe';

describe('FilterpublicationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpublicationsPipe();
    expect(pipe).toBeTruthy();
  });
});
